export class MixAudioOut {
    enabled: boolean; // 默认：true
    outputGain: number; // 默认：-1000
    outputMute: boolean; // 默认： false
    soundQualityHanding: boolean; // 默认：false
    outputVolume: number; // 默认：0
}
